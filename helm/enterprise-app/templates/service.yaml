apiVersion: v1
kind: Service

metadata:
  name: enterprise-app
  namespace: {{ .Release.Namespace }}

spec:
  selector:
    app: enterprise-app

  ports:
    - port: 80
      targetPort: 5000

  type: {{ .Values.service.type }}
